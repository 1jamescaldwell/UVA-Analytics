{"version":"0.3.0","body":"\nfunction main(workbook: ExcelScript.Workbook) {\n    let selectedSheet = workbook.getActiveWorksheet();\n\n   \n  // Set the last row manually\n  let lastRow = selectedSheet.getRange(\"E6\").getValue() as number; // Cast to number\n\n    // Use the input to dynamically create the range to be sorted\n    let rangeC = `C7:D${lastRow}`;\n    let rangeF = `F7:G${lastRow}`;\n    let rangeI = `I7:J${lastRow}`;\n    let rangeL = `L7:M${lastRow}`;\n    let rangeO = `O7:P${lastRow}`;\n\n    // Sort the ranges with the inputted last row\n    selectedSheet.getRange(rangeC).getSort().apply([{ key: 0, ascending: true }], false, false, ExcelScript.SortOrientation.rows);\n    selectedSheet.getRange(rangeF).getSort().apply([{ key: 0, ascending: true }], false, false, ExcelScript.SortOrientation.rows);\n    selectedSheet.getRange(rangeI).getSort().apply([{ key: 0, ascending: true }], false, false, ExcelScript.SortOrientation.rows);\n    selectedSheet.getRange(rangeL).getSort().apply([{ key: 0, ascending: true }], false, false, ExcelScript.SortOrientation.rows);\n    selectedSheet.getRange(rangeO).getSort().apply([{ key: 0, ascending: true }], false, false, ExcelScript.SortOrientation.rows);\n}\n\n","description":"","noCodeMetadata":null,"parameterInfo":"{\"version\":1,\"originalParameterOrder\":[],\"parameterSchema\":{\"type\":\"object\",\"default\":{},\"x-ms-visibility\":\"internal\"},\"returnSchema\":{\"type\":\"object\",\"properties\":{}},\"signature\":{\"comment\":\"\",\"parameters\":[{\"name\":\"workbook\",\"comment\":\"\"}]}}","apiInfo":"{\"variant\":\"synchronous\",\"variantVersion\":2}"}